<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <title>Weather Reporting App</title>
    <link href='http://fonts.googleapis.com/css?family=Average|Belgrano' rel='stylesheet' type='text/css' media="all">
    <link href="/css/style.css" rel="stylesheet" type="text/css" media="all" />
</head>

<body>
    <div class="container-fluid" style="width: 500px; margin: 0 auto">
        <div class="row">      
            <div class="col-xs-12">
                <header id="header" role="banner">
                    <h1>Weather Reporting App</h1>
                </header>
            </div>
        </div>

        <div id="main" role="main" class="row">
            <div class="col-xs-12">
                <form action="/" method="post" class="form-horizontal">
                    <div class="form-group">
                        <label for="city" class="col-xs-2 control-label">City</label>
                        <div class="col-xs-10">
                            <input name="city" type="text" placeholder="Enter a City" class="form-control" required>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="country" class="col-xs-2 control-label">Country</label>
                        <div class="col-xs-10">
                            <input name="country" type="text" placeholder="Enter a Country" class="form-control">
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-xs-offset-2 col-xs-10">
                            <input type="submit" value="Get Weather" class="btn btn-primary">
                        </div>    
                    </div>
                </form>
            </div>
        </div>
        <%
            var showHideClass = ((weather !== null || error !== null) ? 'show' : 'hide');
            var successErrorClass = ((weather !== null) ? 'alert-success' : 'alert-danger');
        %>
        <div class="row <%=showHideClass%> <%=successErrorClass%> text-uppercase text-center">
            <div class="col-xs-12">
                <% if(weather !== null){ %>
                    <p>
                        <%= weather %>
                    </p>
                    <% } %>
                    <% if(error !== null){ %>
                        <p class="bg-error">
                            <%= error %>
                        </p>
                    <% } %>
            </div>
        </div>

        <!--<div class="row">
            <div class="col-xs-12">
                <footer id="footer" role="contentinfo">
                    <h4>Saddham</h4>
                </footer>
            </div>
        </div>-->
    </div>    
</body>

</html>